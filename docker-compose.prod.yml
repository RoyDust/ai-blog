services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: my-next-app:latest
    env_file:
      - .env
    environment:
      NODE_ENV: production
    ports:
      - "127.0.0.1:3000:3000"
    restart: unless-stopped
